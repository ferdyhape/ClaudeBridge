{
  "name": "claude-submachine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-tenant API server for Claude, with per-account browser login and API key access.",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cross-spawn": "^7.0.3",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mysql2": "^3.24.2"
  }
}
